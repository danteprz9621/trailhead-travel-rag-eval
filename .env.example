# No API key is required -- every agent and every judge/embedder model in
# this project runs locally via Ollama. Kept as a placeholder in case you
# ever point something at a cloud provider instead.
# OPENAI_API_KEY=your-openai-api-key-here
